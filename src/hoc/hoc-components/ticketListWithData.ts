import TicketList from '../../components/logic/ticket-list';
import withStateData from '../hoc-helpers/withStateData';

const TicketListWithData = withStateData(TicketList);

export default TicketListWithData;
